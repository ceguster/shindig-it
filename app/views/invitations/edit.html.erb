<div class="rsvp">
  <h2> RSVP to <%= @event.name %> </h2>
  <%= form_for [@event, @invitation], :remote => true do |f| %>
    <%= f.submit "Attending" %>
    <%= f.submit "Not Attending" %>
    <%= f.submit "Maybe Attending" %>
  <% end %>
</div>